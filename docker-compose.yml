services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app
      - /app/node_modules
      - db-data:/app/data

  frontend:
    build: ./frontend
    ports:
      - "8080:80"

volumes:
  db-data:


